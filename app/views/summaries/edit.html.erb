<h1> Edit Summary </h1>

<%= render "form" %>

<%= link_to "Show", summary_path(@summary) %> |
<%= link_to "Back", summaries_path %> |
<%= link_to "Delete", @summary, :confirm => "Are you sure you want to delete this product?", :method => :delete %>


<%= form_for @summary do |f| %>

<% if @summary.errors.any? %>
	<div id="error explanation">
		<h2><%= pluralize(@summary.errors.count, "error") %> could not be saved: </h2>

		<% @summary.errors.full_messages.each do |msg| %>
			<%= msg %>
		<%end%>
<%end%>

	<div class="field">
		<p>
			<%= f.label :price %>
			<%= f.text_field :price %>
	</div>


	<div class ="field">
		<p>
			<%= f.label :gift_card %>
			<%= f.text_field :gift_card %>
		</p>
	</div>

	<div class ="field">
		<p>
			<%= f.label :promotion_link %>
			<%= f.text_field :promotion_link %>
		</p>
	</div>

	<div class="field">
		<p>
			<%= f.label :contract_id %>
			<%= f.text_field :contract_id %>
		</p>
	</div>

	<div class="field">
		<p>
			<%= f.label :device_id %>
			<%= f.text_field :device_id %>
		</p>
	</div>	

	<div class="field">
		<p>
			<%= f.label :retailer_id %>
			<%= f.text_field :retailer_id %>
		</p>
	</div>
	
	<div class="field">
		<%= f.submit %>
	</div>

<%end%>
